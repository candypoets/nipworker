include "../common.fbs";

namespace nostr.fb;

table Kind7376Parsed {
  direction: string (required);
  amount: int32;
  created_events: [string] (required);
  destroyed_events: [string] (required);
  redeemed_events: [string] (required);
  tags: [HistoryTag];
  decrypted: bool;
}
