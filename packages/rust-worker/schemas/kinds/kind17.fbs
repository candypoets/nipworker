include "../common.fbs";

namespace nostr.fb;

table Kind17Parsed {
  reaction_type: ReactionType;
  event_id: string (required);
  pubkey: string (required);
  event_kind: ulong;
  emoji: Emoji;
  target_coordinates: string;
}
