include "../common.fbs";

namespace nostr.fb;

table Kind1Parsed {
  parsed_content: [ContentBlock] (required);
  shortened_content: [ContentBlock];
  profile_mentions: [ProfilePointer];
  event_refs: [EventPointer];
  address_refs: [AddressPointer];
  reply: EventPointer;
  root: EventPointer;
}
