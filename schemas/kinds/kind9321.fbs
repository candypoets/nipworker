include "../common.fbs";

namespace nostr.fb;

table Kind9321Parsed {
  id: string (required);
  amount: int32;
  recipient: string (required);
  sender: string (required);
  event_id: string;
  mint_url: string (required);
  redeemed: bool;
  proofs: [Proof] (required);
  comment: string;
  is_p2pk_locked: bool;
  p2pk_pubkey: string;
}
