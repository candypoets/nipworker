include "../common.fbs";

namespace nostr.fb;

table Kind7376Parsed {
  direction: string (required);
  amount: int32;
  created_events: [string];
  destroyed_events: [string];
  redeemed_events: [string];
  tags: [HistoryTag];
  decrypted: bool;
}
